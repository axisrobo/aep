# AEP Python Reference

Python is the second-priority reference implementation.

Planned scope:

- Dataclasses or Pydantic models for the AEP envelope
- Runtime validation aligned with shared schemas and TypeScript fixtures
- Subscription matching parity with `reference/typescript/`
- Stdio harness for newline-delimited JSON
- Pytest conformance tests using shared fixtures

No Python package is implemented yet.
