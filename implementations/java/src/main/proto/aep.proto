syntax = "proto3";

package harmovela.v1;
option java_outer_classname = "Harmovela";

service HarmovelaTransport {
  rpc Stream(stream HarmovelaMessage) returns (stream HarmovelaMessage);
}

message HarmovelaMessage {
  string json_payload = 1;
}
